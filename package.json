{
  "name": "liascript-board-mode",
  "version": "0.0.1",
  "description": "LiaScript plugin: full-width presentation layout + font-size slider (Tafelmodus)",
  "scripts": {
    "build": "parcel build src/index.ts --dist-dir dist --no-scope-hoist"
  },
  "devDependencies": {
    "parcel": "^2.13.3",
    "typescript": "^5.8.3"
  }
}
